package oca.samples;

public class StringBuilderOAC {
	
	//Creates empty builder, capacity 16
	 StringBuilder st = new StringBuilder();
	StringBuilder st2 = new StringBuilder("1234");
	
	//Creates an empty string builder swith the specified initial capacity
	StringBuilder	st3=new StringBuilder(30);
	
	//Creates a string builder whose value is initialized by the specified string, plus an extra 16 empty elements trailing the string
	StringBuilder	st4=new StringBuilder("Initial capacity + 16 extra");


}
